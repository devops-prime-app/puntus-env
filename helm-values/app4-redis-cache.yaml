architecture: standalone
auth:
  enabled: false
master:
  persistence:
    enabled: true
    existingClaim: "redis-pvc-does-not-exist"  # DELIBERATE: non-existent PVC
    size: 8Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"
